services:
  - type: web
    name: seu-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: seu-banco
          property: connectionString
      - key: SECRET_KEY
        generateValue: true